apiVersion: apps/v1
kind: Deployment
metadata:
  name: animal-website
  labels:
    app: animal-website
    type: farm  
spec:
  replicas: 2
  selector:
    matchLabels:
      app: animal-website
  template:
    metadata:
      labels:
        app: animal-website
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
        volumeMounts:
        - name: website-html
          mountPath: /usr/share/nginx/html/index.html
          subPath: index.html
        - name: images-volume
          mountPath: /usr/share/nginx/html/images
      volumes:
      - name: website-html
        configMap:
          name: website-html
      - name: images-volume
        persistentVolumeClaim:
          claimName: animal-images-pvc